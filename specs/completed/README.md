# Completed Spec Kit runs

These feature directories are frozen C0-C2 decision and acceptance evidence:

- `001-core-baseline-evidence`
- `002-runtime-ownership`
- `003-bounded-output-contracts`

They are not active specifications and cannot be selected by inference. Their
internal paths and historical references are preserved as recorded, so some
relative links no longer resolve after relocation. Current work starts from the
[Machine delivery plan](../../docs/product/MACHINE_DELIVERY_PLAN.md) and is
named explicitly by `.specify/feature.json`.

